import React from 'react';
import {View, TouchableOpacity, Text} from 'react-native';

const Header = () => {
  return (
    <View>
      <Text>header</Text>
    </View>
  );
};

export default Header;
